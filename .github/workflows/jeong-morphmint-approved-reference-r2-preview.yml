name: JEONG MorphMint Approved Reference R2 Preview

on:
  pull_request:
    branches: [main]
    paths:
      - "global_shorts_render_lab/github_public_runner/morphmint_reference_r2_runner.py"
      - ".github/workflows/jeong-morphmint-approved-reference-r2-preview.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  render-r2-preview:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.ref || github.ref_name }}
      - name: Install Blender runtime libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libgl1 libxrender1 libxi6 libxkbcommon0 libxfixes3 libxxf86vm1
      - name: Download Blender 5.2.2
        run: |
          mkdir -p .cache/blender
          curl -L --fail --retry 3 -o .cache/blender/blender.tar.xz https://download.blender.org/release/Blender5.2/blender-5.2.2-linux-x64.tar.xz
          tar -xf .cache/blender/blender.tar.xz -C .cache/blender
      - name: Render R2 Cycles stills
        run: |
          .cache/blender/blender-5.2.2-linux-x64/blender --background --factory-startup --python global_shorts_render_lab/github_public_runner/morphmint_reference_r2_runner.py
      - name: Verify R2 outputs
        run: |
          test -s render_output/morphmint_reference_r2/ceramic.png
          test -s render_output/morphmint_reference_r2/chrome.png
          test -s render_output/morphmint_reference_r2/crystal.png
          grep -q "MORPHMINT_APPROVED_REFERENCE_R2_STILLS_PASS" render_output/morphmint_reference_r2/result.json
      - name: Upload R2 preview artifact
        uses: actions/upload-artifact@v4
        with:
          name: morphmint-approved-reference-r2-preview
          path: render_output/morphmint_reference_r2/
          if-no-files-found: error
          retention-days: 7
