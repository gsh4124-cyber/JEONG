"use client";
import styles from "./jeong-hero.module.css";
type Props={title:string;isHome:boolean;isReview:boolean;theme:"light"|"dark";dateLabel:string;timeLabel:string;weatherLoading:boolean;weatherIcon:string;weatherTemperature:string;weatherLabel:string;weatherDetail:string;onWeather:()=>void;onSearch:()=>void;onTheme:()=>void;onMenu:()=>void};
export function JeongHero(p:Props){return <header className={`topHeader ${styles.hero} ${p.isReview?styles.reviewHero:""}`}>
 <div className={`headerIdentity ${styles.identity}`}><div className={`greetingMark themeCelestial ${p.theme==="dark"?"celestialMoon":"celestialSun"} ${styles.celestial}`} aria-hidden="true"><span/></div><div className={styles.titleCopy}><h1>{p.title}</h1>{p.isHome&&<small>오늘도 선택하는 하루가 당신의 미래를 만듭니다.</small>}{p.isReview&&<small>하루를 돌아보고<br/>다음을 정합니다.</small>}</div></div>
 <div className={`headerRight ${styles.right}`}><div className={`dateWeatherBlock ${styles.dateWeather}`}><button className={`weatherBlock ${styles.weather}`} onClick={p.onWeather} title="날씨 새로고침"><div className={styles.weatherCondition}><b>{p.weatherLoading?"…":p.weatherIcon}</b><em>{p.weatherLoading?"확인 중":p.weatherLabel}</em></div><span className={styles.weatherStatus}><strong>{p.weatherLoading?"—":p.weatherTemperature}</strong><small>{p.weatherDetail}</small></span><span className={styles.weatherClock}><time>{p.timeLabel}</time><small>{p.dateLabel}</small></span></button></div><div className={`headerTools ${styles.tools}`}><button className="searchTrigger" onClick={p.onSearch}>전체 검색 <kbd>Ctrl+K</kbd></button><button className="iconBtn visibleIconBtn" aria-label="테마 전환" onClick={p.onTheme}><span>{p.theme==="light"?"☾":"☀"}</span></button><button className="iconBtn visibleIconBtn" aria-label="아침 기록 열기" onClick={p.onMenu}><span>☰</span></button></div></div>
 </header>}
